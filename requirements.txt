# Core FastAPI dependencies

fastapi
uvicorn[standard]
gunicorn

# AI and ML
openai
pydantic

# Database
sqlalchemy
psycopg2-binary
alembic

# File processing
python-multipart
pytesseract
pillow
google-cloud-documentai
google-auth

# Caching and session management
redis
python-jose[cryptography]

# HTTP client
httpx
requests

# Logging and monitoring
structlog
python-json-logger

# Environment and configuration
python-dotenv
pydantic-settings

# Testing (for development)
pytest
pytest-asyncio
httpx

# Security
passlib[bcrypt]
python-multipart

# Utilities
python-dateutil
pytz

# Core dependencies (existing)
google-cloud-documentai>=2.0.0
fastapi>=0.68.0
python-multipart>=0.0.5
uvicorn>=0.15.0
pydantic>=1.8.0

# File conversion dependencies (new)
# Document conversion
docx2pdf>=0.1.8          # Word documents to PDF (Windows/macOS)
python-docx>=0.8.11      # Alternative for Word documents
pypandoc>=1.5            # Universal document converter (requires pandoc)

# Excel/CSV conversion
openpyxl>=3.0.9          # Excel file processing
xlrd>=2.0.1              # Legacy Excel files
pandas>=1.3.0            # Data processing for CSV/Excel

# PDF creation
reportlab>=3.6.0         # PDF generation
PyPDF2>=2.10.0           # PDF manipulation

# PowerPoint conversion
python-pptx>=0.6.21      # PowerPoint file processing

# Image processing
Pillow>=8.3.0            # Image processing and conversion

# Text processing
chardet>=4.0.0           # Character encoding detection
python-magic>=0.4.24     # File type detection

# Optional: Advanced conversions
# unoconv>=0.9.0         # LibreOffice-based converter (requires LibreOffice)
# comtypes>=1.1.10       # Windows COM interface (Windows only)

# Development dependencies
pytest>=6.2.0
pytest-asyncio>=0.15.0